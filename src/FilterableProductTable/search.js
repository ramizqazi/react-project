import React from "react";

const SearchBar = () => {
    return (
        <div>
            <input type="search" />
            <div>
                <input type="checkBox" />
                Only show product in stock
            </div>
        </div>
    );
}

export default SearchBar;